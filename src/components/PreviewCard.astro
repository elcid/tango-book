---
const { title, content, image, videoUrl, videoCaption } = Astro.props;
---
<div class="bg-white p-4 md:p-6 rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300">
    <div class="relative aspect-[16/9] mb-4">
        <img
                src={image}
                alt={title}
                class="w-full h-full object-cover rounded-lg"
                loading="lazy"
        />
    </div>
    <h3 class="text-lg md:text-xl font-semibold mb-2">{title}</h3>
    <p class="text-gray-600 text-sm md:text-base mb-3">{content}</p>
    <a href={videoUrl} class="text-blue-600 hover:text-blue-700 underline mb-2 inline-block text-sm md:text-base">
        Watch Video on YouTube
    </a>
    <p class="text-gray-500 text-xs md:text-sm italic">{videoCaption}</p>
</div>